import { DirectiveHook, Plugin } from 'vue';
import { SnipOptions, OnSnipped } from 'js-snip';

interface VueSnipOptions extends SnipOptions {
    onSnipped: OnSnipped;
}
declare type VueSnipDirectiveHook = DirectiveHook<HTMLElement, null, Partial<VueSnipOptions>>;

declare const _default: Plugin;

export { VueSnipDirectiveHook, VueSnipOptions, _default as default };
